{% set route = app.request.get('_route') %}

<nav class="navbar navbar-expand-lg navbar-light bg-primary navbar-dark">
    <a href="#" onclick="window.sidenav.toggle()" class="pr-2 h5 mb-0">
        <i class="fa fa-bars" aria-hidden="true"></i>
    </a>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse py-3 py-md-0" id="navbarSupportedContent">

        <div class="container">
            <div class="row">
                <div class="col-12">
                    <ul class="navbar-nav mr-auto">
                        {% if is_granted('ROLE_ADMIN') %}
                            <li class="nav-item pl-3 pl-md-0 {% if route == 'security_logout' %}active{% endif %}">
                                <a class="nav-link" href="{{ path('security_logout') }}">Logout</a>
                            </li>
                        {% endif %}
                    </ul>
                </div>
            </div>
        </div>
    </div>
</nav>
