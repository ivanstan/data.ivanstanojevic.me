{% extends 'pages/navigation.html.twig' %}

{% block body %}

    {{ parent() }}

    {% block messages %}
        {% if app.request.hasPreviousSession and app.session.flashbag.peekAll() is not empty %}
            <div class="container">
                <div class="row">
                    <div class="col-12">
                        {% include 'components/messages.html.twig' %}
                    </div>
                </div>
            </div>
        {% endif %}
    {% endblock %}

    {% block content %}{% endblock %}

{% endblock %}
