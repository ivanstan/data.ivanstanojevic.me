{% extends 'pages/container.html.twig' %}

{% block title %}{% trans %}settings.title{% endtrans %} | {{ parent() }}{% endblock %}

{% block content %}
    <div class="container">
        <div class="row">
            <div class="col-12">
                <h3 class="mt-3">{% trans %}settings.change_password{% endtrans %}</h3>
                {{ include('components/messages.html.twig') }}
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <button class="btn btn-primary">{% trans %}Submit{% endtrans %}</button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
