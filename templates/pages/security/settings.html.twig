{% extends 'pages/container.html.twig' %}

{% block title %}{{ 'settings.title'|trans }} | {{ parent() }}{% endblock %}

{% block content %}
    <div class="container">
        <div class="row">
            <div class="col-12">
                <h3 class="mt-3">{{ 'settings.change_password'|trans }}</h3>
                {{ include('components/messages.html.twig') }}
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <button class="btn btn-primary">{{ 'general.submit'|trans }}</button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
