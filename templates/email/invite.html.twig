{% extends 'email/base.html.twig' %}

{% set url = url|default('#') %}

{% block title %}{{ subject|default('Email') }} | {{ parent() }}{% endblock %}

{% block content %}
    <tr>
        <td>
            <p>{{ "invite.heading"|trans({'%app_name%': app_name}, 'email') }}</p>
            <p>{{ "invite.explanation"|trans({}, 'email') }}</p>
            <a class="btn btn-primary" href="{{ url }}">{{ 'invite.cta'|trans({}, 'email') }}</a>
            <p>{{ "invite.footer"|trans({}, 'email') }}</p>
        </td>
    </tr>
{% endblock %}
